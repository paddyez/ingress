package org.paddy.rest.client;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGeoResourceIT extends GeoResourceTest {

    // Execute the same tests but in native mode.
}